*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# JS/Node
node_modules/
dist/
.expo/
.expo-export-*/

# Android build outputs
android/app/build/
android/.gradle/

# Environment secrets
.env

# Signing keystores
*.keystore

# Firebase config (contains API keys)
google-services.json
*.jks

# APK binaries
*.apk
*.aab

# Firebase Admin SDK / service account keys
*firebase-adminsdk*
*-firebase-adminsdk-*.json

# Config files with secrets
config.yaml
.asset-manifest.json

# Cloudflare Workers
worker/node_modules/
worker-actions/node_modules/

# LightRAG
infra/.env.lightrag
infra/data/lightrag/
# Firebase config (also listed earlier — keeping a deduplicated entry here)
google-services.json

# dApp Store publish cache (auto-written by scripts/publish-dapp-store.sh)
.last_published_version_code
.last_release_id

# Claude Code local config (machine-specific: paths, permissions, locks)
.claude/

# Image/asset backups (cruft; .bak2 is often a JPEG — AAPT2 trap)
*.bak
*.bak2

